version: "3.7"
services:
  # https://hub.docker.com/r/phntxx/dashboard
  dashboard:
    image: phntxx/dashboard:latest
    container_name: dashboard
    restart: unless-stopped
    ports:
      - 8182:8080
    expose:
      - "8080"
    volumes:
      - ./config/dashboard:/app/data
