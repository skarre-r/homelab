---
apiVersion: "argoproj.io/v1alpha1"
kind: "Application"
metadata:
  name: "calibre-web"
  namespace: "argocd"
spec:
  destination:
    name: "in-cluster"
    namespace: "calibre"
  project: "default"
  syncPolicy:
    automated:
      enabled: false
    syncOptions:
      - "RespectIgnoreDifferences=true"
  ignoreDifferences:
    - kind: "Secret"
      jsonPointers:
        - "/data"
        - "/stringData"
  source:
    repoURL: "https://github.com/skarre-r/homelab.git"
    path: "manifests/apps/calibre-web"
    targetRevision: "HEAD"
