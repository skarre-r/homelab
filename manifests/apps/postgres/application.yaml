---
apiVersion: "argoproj.io/v1alpha1"
kind: "Application"
metadata:
  name: "postgres"
  namespace: "argocd"
spec:
  destination:
    name: "in-cluster"
    namespace: "postgres"
  project: "default"
  syncPolicy:
    automated:
      enabled: false
    syncOptions:
      - "RespectIgnoreDifferences=true"
  ignoreDifferences:
    - kind: "Secret"
      jsonPointers:
        - "/data"
        - "/stringData"
  source:
    repoURL: "https://github.com/skarre-r/homelab.git"
    path: "manifests/apps/postgres"
    targetRevision: "HEAD"
