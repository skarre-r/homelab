---
apiVersion: "argoproj.io/v1alpha1"
kind: "Application"
metadata:
  name: "overseerr"
  namespace: "argocd"
spec:
  destination:
    name: "in-cluster"
    namespace: "overseerr"
  project: "default"
  syncPolicy:
    automated:
      enabled: false
    syncOptions:
      - "RespectIgnoreDifferences=true"
  ignoreDifferences:
    - kind: "Secret"
      jsonPointers:
        - "/data"
        - "/stringData"
  source:
    repoURL: "https://github.com/skarre-r/homelab.git"
    path: "manifests/apps/overseerr"
    targetRevision: "HEAD"
