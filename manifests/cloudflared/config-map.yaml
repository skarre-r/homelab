---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "cloudflared"
  namespace: "cloudflare"
data:
  config.yaml: |-
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
