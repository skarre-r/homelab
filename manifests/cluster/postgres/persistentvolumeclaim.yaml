---
apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "postgres"
  namespace: "postgres-system"
spec:
  volumeName: "postgres"
  volumeMode: "Filesystem"
  accessModes:
    - "ReadWriteOnce"
  storageClassName: "local-path"
  resources:
    requests:
      storage: "1Gi"
